{
  "name": "json-server-backend",
  "version": "1.0.0",
  "description": "Backend con JSON Server",
  "scripts": {
    "server": "json-server --watch data/data.json --port 3000",
    "start": "npm run server"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}
